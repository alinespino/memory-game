import React from "react";


const Header = props => (
  <header className="header">
    <h1>Memory Game!</h1>
    <h2>
      Click on an image no more than once!
    </h2>
  </header>
);

export default Header;